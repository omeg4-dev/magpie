[Unit]
Description=Index new screenshots hourly

[Timer]
# Hourly rather than on a path watch: a screenshot you took a minute ago is
# still on the clipboard, so the browser is the slow path by definition.
OnBootSec=2min
OnUnitActiveSec=1h
Persistent=true

[Install]
WantedBy=timers.target
